Dear <%= @signature.name %>,

We noticed that you tried to sign the petition "<%= @petition.action %>" but haven't confirmed your email address.

Click this link to sign the petition:

<%= verify_signature_url(@signature, @signature.perishable_token) %>

Thanks,
<%= t("petitions.emails.signoff_prefix") %>
<%= t("petitions.emails.signoff_suffix") %>
