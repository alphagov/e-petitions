<%= render 'parliament_dissolution_warning' %>

<%= form_for @signature, url: new_petition_signature_path(@petition) do |form| %>
  <%= hidden_field_tag :stage, @signature.stage %>
  <%= render @signature, form: form, signature: form.object %>
<% end %>
