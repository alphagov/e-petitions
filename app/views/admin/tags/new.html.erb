<h1>New Tag</h1>

<div class="grid-row">
  <div class="column-two-thirds extra-gutter">
    <%= form_for [:admin, @tag] do |form| %>
      <%= render 'form', form: form %>
    <% end %>
  </div>
</div>
