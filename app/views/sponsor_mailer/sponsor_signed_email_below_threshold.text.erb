Dear <%= @petition.creator_signature.name %>,

<%= @sponsor.signature.name %> supported your petition – "<%= @petition.action %>".

You have <%= pluralize(@supporting_sponsors_count, 'supporter') %> so far. If <%= @moderation_threshold %> people support your petition we’ll check it to make sure it meets the petition standards. If it does, we’ll publish it.

Find out how we check petitions before we publish them:
<%= help_url(anchor: 'standards') %>

Thanks,
<%= t("petitions.emails.signoff_prefix") %>
<%= t("petitions.emails.signoff_suffix") %>
