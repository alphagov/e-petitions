<%= form_for @signature, url: new_petition_sponsor_path(@petition, token: @petition.sponsor_token) do |form| %>
  <%= hidden_field_tag :stage, @signature.stage %>
  <%= render @signature, form: form, signature: form.object %>
<% end %>
